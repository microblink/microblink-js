import Microblink from '../src/microblink'

/**
 * Default test
 */
describe('Microblink test', () => {
  it('Microblink class is instantiable', () => {
    expect(new Microblink()).toBeInstanceOf(Microblink)
  })
})
